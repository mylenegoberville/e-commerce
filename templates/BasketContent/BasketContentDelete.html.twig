<form method="post" action="{{ path('basketContent_delete', {'id': basketContent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ basketContent.id) }}">
    <button class="btn">{{ 'button.delete' |trans}}</button>
</form>
